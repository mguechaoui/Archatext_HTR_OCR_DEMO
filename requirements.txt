fastapi>=0.111,<0.116
uvicorn[standard]>=0.30,<0.35
pydantic>=2.7,<3
pydantic-settings>=2.3,<3
python-multipart>=0.0.9
Pillow>=10.3,<12
# kraken 5.3.0 requires numpy~=2.0.0; torch 2.4.1 and matplotlib 3.8+ both
# support numpy 2.x, so this is not a compromise anywhere else in the stack.
numpy>=2.0,<2.1
matplotlib>=3.8,<4

# Artifact fetching (Azure Blob over HTTPS + SAS; no azure SDK needed)
httpx>=0.27,<0.29

# Segmentation + OCR (kraken). Pinned exact, not a range: letting pip search
# across kraken's numpy~=1.23 (<=5.2.9) and numpy~=2.0.0 (5.3.0) versions is
# what triggered the backtracking/ResolutionImpossible failure above.
kraken==5.3.0

# Torch is installed from the CPU-only index in the Dockerfile.
# Kept here (pinned) so a bare `pip install -r requirements.txt` still works.
torch==2.4.1
torchvision==0.19.1

# HATFormer / transformer OCR engine
transformers>=4.41,<5